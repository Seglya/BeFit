@using Microsoft.AspNetCore.Routing@model List<BeFit.Models.UserProfileViewModels.AddMeasurementViewModel>

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete meal for @ViewData["Date"]?</h3>
<div>
    
    <hr/>
    <form asp-action="Delete" asp-route-user="@ViewData["user"]">
       
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default"/> |
            <a asp-action="Detail" asp-route-id="@ViewData["oneDayId"]">Back to Detail</a>
        </div>
    </form>
</div>